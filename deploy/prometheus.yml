scrape_configs:
  - job_name: sybil-api
    scrape_interval: 5s
    metrics_path: /metrics
    static_configs:
      # In docker-compose: sybil-api:3000
      # In Kamal: host.docker.internal:3000 (port is exposed on host)
      - targets: ["host.docker.internal:3000"]
