NEXT_PUBLIC_API_BASE=https://api.sybil.exchange
NEXT_PUBLIC_WS_BASE=wss://api.sybil.exchange
# Registrable domain, not the app host. Must match EXPECTED_WEBAUTHN_RP_ID in
# crates/sybil-verifier/src/key_op_auth.rs or the guest rejects every assertion.
NEXT_PUBLIC_WEBAUTHN_RP_ID=sybil.exchange
NEXT_PUBLIC_SENTRY_DSN=
