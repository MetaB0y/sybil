# Implemented design history

These documents explain the design lineage behind account-key commitments,
WebAuthn guest verification, witness transitions, escape claims, and recovery.
Their essential work has landed, but their version numbers, file paths, status
sections, and future-tense instructions are historical.

Use `docs/SPEC.md`, the verification/recovery architecture notes, ADRs, code,
and tests for current behavior. Do not execute these plans again.
